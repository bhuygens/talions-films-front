const Tabs = {
  clips: "clip",
  events: "evenement",
  pubs: "pub",
};

const ImagesConfig = {
  imageWidth: 250,
  maxImageHeight: 400,
  imagesPerLine: 10,
}

const IconConfig = {
  width: 0
}
export {Tabs, ImagesConfig}
